<h1>タスク一覧</h1>
<h2>ID番号をクリックすることで詳細ページへ遷移します。</h2>

<ol>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.content %></li>
  <% end %>
</ol>

<%= link_to "タスクの追加", new_task_path %>